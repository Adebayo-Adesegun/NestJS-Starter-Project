NODE_ENV=development
PORT=9000

# CORS: Specify allowed origins (comma-separated for multiple, or * for dev only)
# SECURITY WARNING: Do NOT use '*' in production
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# AWS S3 Configuration (optional)
AWS_BUCKET=aws-bucket-name
AWS_REGION=eu-west-1
AWS_ACCESS_KEY=aws-access-key
AWS_SECRET_KEY=aws-secret-key

# Database Configuration
# SECURITY: Use strong passwords in production (min 12 chars)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=nestjs_user
DATABASE_PASSWORD=your-very-secure-password-min-12-chars
DATABASE_NAME=nestjs_db
DATABASE_SSL=false

# Mail Configuration
# Choose a transport. Common options:
# SMTP: specify host, port, user, pass
# Or full URL: MAIL_TRANSPORT=smtp://user:pass@smtp.mailtrap.io:2525
MAIL_TRANSPORT=
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_SECURE=false
MAIL_USER=
MAIL_PASSWORD=
MAIL_FROM="Nest Starter <no-reply@example.com>"
MAIL_PREVIEW=true
MAIL_TEMPLATE_PATH=./src/mailer/templates

# JWT Configuration
# SECURITY: Use a strong secret (min 32 chars) and rotate regularly
JWT_SECRET=your-very-secure-jwt-secret-min-32-characters-long
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# Password Reset Configuration
# SECURITY: Customize the front-end URL and token expiry time
FRONTEND_URL=http://localhost:3000
PASSWORD_RESET_EXPIRES_IN=60

# Redis Configuration (REQUIRED)
# SECURITY: Redis is required for distributed rate limiting
# Connection format: redis://[user:password@]host:port[/db]
# For local development: redis://localhost:6379
# For production: use managed Redis service (AWS ElastiCache, Redis Cloud, etc.)
REDIS_URL=redis://localhost:6379

# JWT
JWT_SECRET=change-me
JWT_EXPIRES_IN=1h
